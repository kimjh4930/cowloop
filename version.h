/*
** This include-file contains the common version number
** for the cowloop package distribution.
**
** Note that the driver source 'cowloop.c' has this same revision
** number hard coded. We could not use an #include "version.h"
** in the driver because that would conflict with the possibility
** to move the driver to the main kernel tree.
**
** $Log: version.h,v $
*/

char revision[] = "$Revision: 3.1 $";
